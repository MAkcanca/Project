<% if @department.courses.empty? %>
	<em>No departments</em>
<% else %>
	<ul>
		<% @department.courses.where('semester_id = ?', @current_semester.id).each do |c| %>
			<li><%= link_to c.title, course_path(c) %></li>
		<% end %>
	</ul>
<% end %>
